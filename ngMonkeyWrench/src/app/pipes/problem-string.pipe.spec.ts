import { ProblemStringPipe } from './problem-string.pipe';

describe('ProblemStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ProblemStringPipe();
    expect(pipe).toBeTruthy();
  });
});

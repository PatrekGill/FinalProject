import { ActiveCallsPipe } from './active-calls.pipe';

describe('ActiveCallsPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveCallsPipe();
    expect(pipe).toBeTruthy();
  });
});

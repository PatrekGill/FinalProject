import { UserBusinessesPipe } from './user-businesses.pipe';

describe('UserBusinessesPipe', () => {
  it('create an instance', () => {
    const pipe = new UserBusinessesPipe();
    expect(pipe).toBeTruthy();
  });
});

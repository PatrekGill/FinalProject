import { SearchByProblemDescriptionPipe } from './search-by-problem-description.pipe';

describe('SearchByProblemDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByProblemDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});

import { SearchByUsernamePipe } from './search-by-username.pipe';

describe('SearchByUsernamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByUsernamePipe();
    expect(pipe).toBeTruthy();
  });
});

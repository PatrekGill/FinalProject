import { SearchByServicetypeNamePipe } from './search-by-servicetype-name.pipe';

describe('SearchByServicetypeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByServicetypeNamePipe();
    expect(pipe).toBeTruthy();
  });
});

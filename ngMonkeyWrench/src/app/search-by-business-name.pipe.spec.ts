import { SearchByBusinessNamePipe } from './search-by-business-name.pipe';

describe('SearchByBusinessNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByBusinessNamePipe();
    expect(pipe).toBeTruthy();
  });
});
